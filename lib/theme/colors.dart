import 'package:flutter/material.dart';

class AppColors {
  // AppBar
  static const navy = Color(0xFF0A2540);

  // Background
  static const backgroundBeige = Color(0xFFF5E6D3);

  // Search bar
  static const searchBarBackground = Color(0xFFFFF8E1);
  static const searchBorder = Color(0xFFE8D4A0);

  // Filter chips
  static const chipBackground = Color(0xFFFFF8E1);
  static const chipText = Color(0xFF2C2C2C);

  // Text colors
  static const resultText = Color(0xFF6B5D52);
  static const clearFiltersText = Color(0xFF7B68EE);

  // Card
  static const cardBackground = Colors.white;
  static const cardBorder = Color(0xFFD4B896);
  static const badgeBackground = Color(0xFFD4A843);
  static const badgeText = Color(0xFF0A2540);
  static const titleText = Color(0xFF2C2C2C);
  static const summaryText = Color(0xFF6B6B6B);

  // Icons
  static const iconColor = Color(0xFF0A2540);

  // Popup menu
  static const menuBackground = Color(0xFFF5F5F5);
  static const menuIconColor = Color(0xFF2C2C2C);

  // Legacy colors (keep for compatibility)
  static const softYellow = Color(0xFFF7EFCB);
  static const cream = Color(0xFFF4E8C1);
  static const purple = Color(0xFF7A4DF0);
  static const gold = Color(0xFFF2C94C);
  static const white = Colors.white;
}
