Place the launcher image here as a PNG named exactly `launcher.png`.

How to add the image (quick):
- In VS Code: open the Explorer, open this project, right-click the `assets` folder -> "Reveal in File Explorer".
- Save or copy the image file from your downloads into that `assets` folder and confirm its name is `launcher.png`.

Required path in repo:
`assets/launcher.png`

After you paste the image, reply here with "done" and I'll generate Android mipmap icons and finish wiring the splash and launcher icon.
